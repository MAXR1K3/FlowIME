<Page
    x:Class="FlowIME.App.Views.InputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Auto"
        HorizontalContentAlignment="Stretch">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="InputSpacingStates">
                <VisualState x:Name="CompactInputSpacing">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PageContent.Padding" Value="16,20,16,28" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ComfortableInputSpacing">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="760" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PageContent.Padding" Value="24,24,24,32" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideInputSpacing">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1100" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PageContent.Padding" Value="32,28,32,40" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <StackPanel x:Name="PageContent" MaxWidth="{StaticResource FlowSettingsContentMaxWidth}" Padding="16,20,16,28" Spacing="22" HorizontalAlignment="Stretch">


            <StackPanel Spacing="5">
                <TextBlock Text="输入法支持" Style="{StaticResource FlowPageTitleStyle}" />
                <TextBlock
                    Text="这里仅列出可在规则中选择的输入法。输入法的运行状态与技术实现无需用户管理。"
                    Style="{StaticResource FlowPageSubtitleStyle}" />
            </StackPanel>

            <StackPanel Spacing="10">
                <TextBlock Text="可用输入法" Style="{StaticResource FlowSectionTitleStyle}" />

                <Border Style="{StaticResource FlowCardStyle}" Padding="20">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Width="44"
                            Height="44"
                            CornerRadius="10"
                            Background="{ThemeResource FlowInsetBackgroundBrush}">
                            <FontIcon
                                Glyph="&#xE765;"
                                FontSize="20"
                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="1" Spacing="3" VerticalAlignment="Center">
                            <TextBlock Text="微软拼音" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <TextBlock
                                Text="可用于应用规则与全局默认场景。"
                                Style="{StaticResource FlowMutedTextStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource FlowCardStyle}" Padding="20">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Width="44"
                            Height="44"
                            CornerRadius="10"
                            Background="{ThemeResource FlowInsetBackgroundBrush}">
                            <FontIcon
                                Glyph="&#xE8D2;"
                                FontSize="20"
                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="1" Spacing="3" VerticalAlignment="Center">
                            <TextBlock Text="微信输入法" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <TextBlock
                                Text="可用于应用规则与全局默认场景。"
                                Style="{StaticResource FlowMutedTextStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <Border Padding="18" Style="{StaticResource FlowCardStyle}">
                <StackPanel Spacing="5">
                    <TextBlock Text="第三方输入法" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock
                        Text="后续版本会继续扩展可选输入法；新增支持后会直接出现在规则编辑器中。"
                        Style="{StaticResource FlowMutedTextStyle}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
