FlowIME P8B.4.1 — test compile fix

Scope: test-only repair on top of P8B.4.

Fixes:
1. FlowIME.Windows.Tests: removes the public Theory parameter whose type is the internal
   QueryUserNotificationState enum. The same ambiguous shell states are now exercised
   inside a Fact loop, preserving coverage without changing production accessibility.
2. FlowIME.Infrastructure.Tests: every async API that accepts CancellationToken now receives
   TestContext.Current.CancellationToken, satisfying xUnit1051 under TreatWarningsAsErrors.

No production files are changed. Gameplay detection, US-keyboard baseline, hotkey guard,
rule resolution, providers, and UI behavior are identical to P8B.4.
